<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Reklam SDK'sı ve UMP onay mesajı ağ erişimi için gerekli. Belge dönüştürme işlemleri
         tamamen cihaz üzerinde yapılır, bu izin dönüştürme akışında kullanılmaz. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/Theme.EvrakDonusturucu"
        tools:targetApi="35">

        <!-- TODO(Faz 5): AdMob hesabında yeni bir Android uygulaması oluşturulduğunda
             gerçek Android App ID ile değiştirilecek. Şu an Google'ın herkese açık
             test App ID'si kullanılıyor. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:theme="@style/Theme.EvrakDonusturucu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- IncomingFileRouter.swift (.onOpenURL) karşılığı. `*/*` eklenmiyor: bu uygulamayı
                 her "Birlikte aç" listesine sokar (kötü UX + Play politikası açısından sakıncalı).
                 Gerçek UDF doğrulaması kodda yapılır (bkz. IncomingFileFilter) çünkü çoğu
                 content:// sağlayıcı MIME tipini application/octet-stream olarak bildirir ve
                 dosya adını path'e yansıtmaz. -->
            <intent-filter android:label="UDF ile aç">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:mimeType="application/x-udf" />
                <data android:mimeType="application/vnd.uyap.udf" />
                <data android:mimeType="application/octet-stream" />
                <data android:mimeType="application/zip" />
                <data android:mimeType="application/x-zip-compressed" />
            </intent-filter>

            <!-- file:// ve dosya adını yola yansıtan sağlayıcılar için uzantı bazlı eşleşme. -->
            <intent-filter android:label="UDF ile aç">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:host="*" />
                <data android:pathPattern=".*\\.udf" />
                <data android:pathPattern=".*\\..*\\.udf" />
                <data android:pathPattern=".*\\..*\\..*\\.udf" />
                <data android:pathPattern=".*\\..*\\..*\\..*\\.udf" />
            </intent-filter>

            <intent-filter android:label="UDF ile aç">
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/octet-stream" />
                <data android:mimeType="application/zip" />
                <data android:mimeType="application/x-zip-compressed" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
